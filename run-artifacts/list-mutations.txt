# List round — mutation table

Each mutation is one named break, applied to the app (never to a check), its anchor asserted to exist and to CHANGE the file, the suite run, and the original restored and compared byte-for-byte. `RED` lines are the assertions that noticed.

## Phase 1 · every task has a due date — `src/lib/taskDue.test.ts` (27 cases)

M1.1 · the hold is ignored — the ask date comes back
    exit 1 · Tests 2 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > the date they set wins > a 'hold me to it' date overrides the ask date
    RED  × src/lib/taskDue.test.ts > the date they set wins > a hold on a nudge overrides the window, and stays the agent's clock

M1.2 · a housekeeping gap borrows the query's send date — a fabricated due date
    exit 1 · Tests 2 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > materials_unrecorded: no natural date when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > materials_unrecorded_bulk: no natural date when the record holds it · null when it does not

M1.3 · a dated note loses its date
    exit 1 · Tests 1 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > the date they set wins > a dated note's date is its due date; an undated one has none

M1.4 · a YYYY-MM-DD is parsed as UTC midnight
    exit 1 · Tests 1 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > real days, on the local calendar > a YYYY-MM-DD names its own day — never UTC midnight, which is the day before in the Americas

M1.5 · owner by magnitude — old dates become the writer's
    exit 1 · Tests 2 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > whenever the engine would raise a nudge or a close, the due date exists
    RED  × src/lib/taskDue.test.ts > owner follows the task's kind > the same distance from today gives the same owner it would at any other distance

M1.6 · the booked check-in is ignored after a nudge
    exit 1 · Tests 1 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > the date they set wins > the check-in after a nudge is its due date — exactly the day `buildNudgeWrites` booked

M1.7 · a request with no date on record stops being owed
    exit 1 · Tests 5 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > offer_received: arrived when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > partial_requested: ask when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > full_requested: ask when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > revise_resubmit: ask when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > owner follows the task's kind > a request is owed with or without a date; everything else needs a date to be anybody's

M1.8 · the natural date is taken from the send, not the ask
    exit 1 · Tests 5 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > partial_requested: ask when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > full_requested: ask when the record holds it · null when it does not
    RED  × src/lib/taskDue.test.ts > every task type derives a due date or a deliberate null > the natural dates are the days the records name — the ask, the arrival, the window
    RED  × src/lib/taskDue.test.ts > the date they set wins > a 'hold me to it' date overrides the ask date
    RED  × src/lib/taskDue.test.ts > the date they set wins > a mute is not a date, and a dismissal is the later word

## Phase 2 · the list view — the pure cells (`listCells.test.ts`, `todoListView.test.ts`, `taskListWide.test.tsx`)

M2.7 · the weeks/months boundary moves — the contract's own function no longer agrees
    exit 1 · Tests 3 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/listCells.test.ts > Overdue by — the contract's unit function > is the contract's own function, at every day from 0 to 4,000
    RED  × src/lib/listCells.test.ts > Overdue by — the contract's unit function > the six probe days — each the contract's answer
    RED  × src/lib/listCells.test.ts > Overdue by — the contract's unit function > and the boundaries where the contract's rounding actually puts them

M2.8 · the chip states its year always, including this year
    exit 1 · Tests 2 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/listCells.test.ts > the Due chip — month, day, and the year only when it is not this year > this year states no year; another year states it; no day is its own chip
    RED  × src/components/todo/taskListWide.test.tsx > 2 · the wiring — RENDERED, so the claims are about the markup and not the props > Due is the calendar chip: month over day, the year only when it is not this year, dashed for none

M2.9 · the Task cell prints the card's raw title instead of the act
    exit 1 · Tests 1 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/components/todo/taskListWide.test.tsx > 2 · the wiring — RENDERED, so the claims are about the markup and not the props > the task is the act and its category; the agent is always rendered, the agency on its own line

M2.10 · a new head starts flipped rather than in its natural order
    exit 1 · Tests 1 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/todoListView.test.ts > the column heads sort by what the columns print (list round, Phase 2) > a head click: a new head starts in its natural order, the same head flips

M2.11 · the arrow never says which way the numbers run
    exit 1 · Tests 2 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/todoListView.test.ts > the column heads sort by what the columns print (list round, Phase 2) > the arrow says which way the column's own numbers run — ▼ for longest overdue first
    RED  × src/components/todo/taskListWide.test.tsx > 2 · the wiring — RENDERED, so the claims are about the markup and not the props > the header: four heads that SORT, in the contract's order — and the active one says so

M2.12 · the retired verb button comes back to the row
    exit 1 · Tests 2 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/components/todo/taskListWide.test.tsx > 2 · the wiring — RENDERED, so the claims are about the markup and not the props > the contract's five cells, by their names — and the retired cells gone, bounded
    RED  × src/components/todo/taskListWide.test.tsx > 2 · the wiring — RENDERED, so the claims are about the markup and not the props > the row keeps ONE control — the ⋯, the snooze door; the verb and the × are not rebuilt

M2.13 · the undated sort first instead of last
    exit 1 · Tests 1 ⎯⎯⎯⎯⎯⎯⎯
    RED  × src/lib/todoListView.test.ts > the column heads sort by what the columns print (list round, Phase 2) > Overdue by: real days, longest first — then due today, the soonest ahead, the undated last

## Phase 2 · the list view — the RENDERED page (`listRound.measure.ts`, 16 claims)

M2.1 · the row's five cells lose a track — the header and the row stop agreeing
    (proved · see run-artifacts/list-round.txt at the time · P2.1b/P2.1c red)
M2.5 · the header's tracks stop matching the row's — `.lhd` 220px → 200px
    14/16
    RED  P2.1b · the header and the row resolve the SAME six tracks
             header 5px 481px 200px 92px 170px 40px · row 5px 461px 220px 92px 170px 40px
    RED  P2.1c · each head sits over its own column, to the pixel
             heads [306,805,1023,1133] · cells [306,785,1023,1133]
    ⚠️ THIS WAS "NOTHING WENT RED" FOR TWO RUNS AND THE CAUSE WAS NOT THE MUTATION. `auth.setup`
    died on `auth/quota-exceeded`, the measurement never started, and the runner read the PREVIOUS
    run's report off disk. Re-run with the report deleted first and the session reused — two claims,
    and the second is the stronger one: a head standing 20px off its own column.

## Phase 3 · the landing state (`todoListView.test.ts`, `taskListWide.test.tsx`, rendered)

M3.1 · "this week" swallows a month                          RED (whenBucket, 3 cases)
M3.2 · the heads run out of chronological order              RED (WHEN_ORDER)
M3.3 · the default goes back to the urgency partition        RED (VIEW_DEFAULT)
M3.4 · finished work is admitted again                       RED (keptGroups, over every grouping)
M3.5 · no head takes the Overdue colour                      RED (rendered)
M3.6 · the Overdue head is painted ink                       RED (rendered)
M3.7 · a first visit lands on urgency instead of When        RED · 3/8 · five claims
M3.8 · the Overdue head is painted like every other head     RED · 7/8 · one claim

## Phase 4 · the grid's card (`taskTicket.test.tsx` + `listRound.measure.ts`, 6 rendered claims)

M4.1 · the headline goes back to the card's own title, which names the agent
    4/6 · RED P4.1 (the agent named twice) · RED P4.2 (the title names them)
M4.3 · the status leaves the top line for the foot            4/6 · RED P4.3
M4.4 · the foot names the person and drops the agency beneath 5/6 · RED P4.4
    ⚠️ P4.4 COULD NOT BE REDDENED UNTIL IT WAS FIXED. It read "the foot names the person, with their
    agency beneath" and asserted only the person — which the population floor five lines above
    already throws on, so the agency half had no fault of its own to fail on. The first two attempts
    at this mutation both died on that floor. It asserts the agency now.
M4.5 · a verb the contract does not print                     5/6 · RED P4.5
M4.6 · the verb is loud at rest rather than on hover          5/6 · RED P4.6
M4.7 · the headline goes back to the card's own title         RED (unit · the title is the ACT)
M4.8 · the status is a dot with no word                       RED (unit · after the fix below)
    ⚠️ UNPROVED TWICE FIRST, AND THE ASSERTION WAS THE FAULT. `StatusDot` renders `aria-label` AND
    `title` carrying the status name, so a card drawing the dot and NO word satisfied a plain
    `toContain` over markup. Stripping `<svg>…</svg>` was not enough — the WRAPPING SPAN holds both
    attributes. The claim is made against the text with every tag removed now, which is the only
    form of it an attribute cannot satisfy.
M4.9  · the verb becomes a nested control inside the card      RED (unit, 2 cases)
M4.10 · a verb the contract does not print                     RED (unit, 2 cases)
M4.11 · a writer's own item stops saying what finishes it      RED (unit)
M4.12 · the snipped panel loses the only place it names anybody RED (unit)
M4.13 · the verb never takes the pink under the pointer        RED (unit)

## Phase 6 · the diff lock (`viewsDiff.measure.ts`)

M6.1 · a selector leaves the parts table
    NO REPORT — and that is correct: the coverage case is a PRECONDITION and refuses to measure.
    Error: design-refs/todo-list-and-card.html: grid declares selectors this lock does not compare
    and does not name a home for
M6.2 · the APP bends a value the contract states (`.tkt` radius 12 → 10)
    244 assertions · DIFF grid .card · Error: grid .card → .tkt differs from the contract
M6.3 · the APP moves an element without changing the moved one's own properties (the foot's avatar
       widened to 60px)
    244 assertions · PLACE grid .card · .card .in · .card .foot
    Error: grid .card → .tkt sits somewhere else inside `.cards`
    ⚠️ M6.2 and M6.3 are the APP's side. The contract's side — a bent value and a moved element on
    the ref's own copy — is `viewsDiff`'s second test and was already proved; both directions now.

## Phase 7 · the one red this round caused, and the one claim it added

M7.1 · the drawer is named the same thing whatever it holds (`SlideOver label` → a constant)
    RED  · P5.2 · the drawer is named for the task it holds
             first "Task" for "Noah Bright" · second "Task" for "Ottoline Frayn"
    ⚠️ THE CLAIM IT REPLACED COULD NOT BE REDDENED BY THIS AT ALL — it compared the drawer's label
    against the ticket's headline, so a drawer named the same thing for every card would have
    matched a ticket headline that was also the same, and a drawer named for the WRONG card would
    have passed the moment the two derivations happened to agree. It asserts identity now: two
    cards, two labels, each naming the person its own card's foot names.

M7.2 · the task cell names the agent the Agent cell already names
    16/17
    RED  · P2.8e · no row's task names the agent its own Agent cell names
             33 rows name an agent · 26 offenders, e.g. ["Ottoline Frayn","Send your full
             manuscript — Ottoline Frayn"], ["Noah Bright","Reply to the offer — Noah Bright"]
    ⚠️ THE FIRST ATTEMPT AT THIS MUTATION NEVER APPLIED, AND THE RUN AFTER IT READ "PASS". The
    anchor matched a `function` declaration where the source has an arrow `const`, the edit silently
    did nothing, and the measurement then reported a clean 17/17 about an unmutated build — the
    "nothing went red" shape arriving by a third route. The assert in the edit script is what named
    it; the run that followed would have recorded a proof that never happened.
